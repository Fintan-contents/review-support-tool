# エラーハンドリング・メッセージ一覧

---

## CleanupOnError パターン

`CmdGen_Click_Core` 内のバリデーション失敗・異常終了時は、共通クリーンアップ Sub `CleanupOnError` を呼び出してから `Exit Sub` する。

```vba
Private Sub CleanupOnError()
    Application.ScreenUpdating = True
    Application.Cursor = xlDefault
End Sub
```

**適用箇所**（`CmdGen_Click_Core` 内）:

| 箇所 | 条件 |
|------|------|
| `LoadCategoryMappings` 失敗 | カテゴリ0件（E12）またはシート未存在 |
| `ValidateReviewListInputs` 失敗 | E01〜E05 いずれか |
| `OpenReviewListFile` 失敗 | E04/E05（ファイル・シート未存在） |
| `AdjustReviewListCategoryColumns` 失敗 | E13（カテゴリ不一致） |
| `GetReviewRecordSheets` 戻り値=2 | E06/E07（レビュー記録票シート問題） |
| `PrepareOutputSheet` 失敗 | E13（出力シートでカテゴリ不一致） |
| `On Error GoTo ERROR` ハンドラ | 予期しない実行時エラー（E11） |

---

## 同じレビュー回数での重複実行

**状況**:

- レビュー回数 = 1 で既にレビュー結果シート「レビュー結果 1 回目」が存在
- 再度レビュー回数 = 1 で実行

**動作**:

確認ダイアログは表示されず、以下を自動的に上書きする。

1. **結果シート**: `Application.DisplayAlerts = False` で既存シートを削除し、新規生成する
2. **レビュー記録票（指摘一覧）**: 同回数の既存行を上書き更新する
3. **レビュー記録一覧**: 同文書ID・同回数の既存行を上書き更新する（新規行は追加しない）

---

## エラーシート構造

| 列 | 項目 | 説明 |
|---|------|------|
| A | エラー内容 | エラーメッセージ |
| B | 場所 | エラーが発生したセルへのハイパーリンク |

---

## メッセージ一覧

### 正常完了通知（MsgBox / vbInformation）

| コード | メッセージ | 表示タイミング | 処理への影響 | 備考 |
|--------|-----------|-------------|------------|------|
| I01 | レビュー結果の抽出が完了しました。処理ファイル数: {N}件 | 抽出処理が正常に完了 | なし（通知のみ） | - |
| I02 | {ファイル名} のメモを削除しました。 | コメント削除が完了 | なし（通知のみ） | - |
| I03 | {ファイル名} のレビュー結果シートを削除しました。 | レビュー結果シート削除が完了 | なし（通知のみ） | - |

### 確認（MsgBox / vbYesNo または vbYesNoCancel）

| コード | メッセージ | 表示タイミング | はい | いいえ/キャンセル | 備考 |
|--------|-----------|-------------|-----|----------------|------|
| C01 | レビュー記録一覧を一旦閉じて開き直します。編集中の内容は保存されます。処理を続行してよろしいですか？ | レビュー記録一覧が開いたまま編集中の場合 | 保存して閉じ、処理続行 | 処理中断 | - |
| C02 | {ファイル名} のレビュー内容を抽出しますか？ | 対象設計書ファイルが見つかるたびに | 抽出処理を実行 | いいえ: そのファイルをスキップ / キャンセル: 以降の全ファイルをスキップ | - |
| C03 | 以降の処理を中止します。 | C02 でキャンセル選択後 | -（通知のみ） | - | - |
| C04 | {ファイル名} のメモを削除してよろしいですか？ | コメント削除ボタン押下時（対象ファイルごと） | コメントを削除 | そのファイルをスキップ | - |
| C05 | {ファイル名} のレビュー結果シートを削除してよろしいですか？ | レビュー結果シート削除ボタン押下時（対象ファイルごと） | レビュー結果シートを削除 | そのファイルをスキップ | - |

### 警告・処理続行（MsgBox / vbExclamation またはエラーシート出力）

| コード | メッセージ | 表示タイミング | 処理への影響 | 備考 |
|--------|-----------|-------------|------------|------|
| W01 | {ファイル名} に対応するレビュー記録票が開いていません。 | 対象設計書に対応するレビュー記録票ファイルが開いていない | そのファイルをスキップして次のファイルへ | - |
| W02 | "工程" を成果物から取得できません。指定したシート名、セルを確認してください。抽出処理は続行します。 | 工程の参照先セルが見つからない | "取得できませんでした" をセットして処理続行 | - |
| W03 | "レビュア" を成果物から取得できません。指定したシート名、セルを確認してください。抽出処理は続行します。 | レビュアの参照先セルが見つからない | "取得できませんでした" をセットして処理続行 | - |
| W04 | "作成者・レビュイ" を成果物から取得できません。指定したシート名、セルを確認してください。抽出処理は続行します。 | レビュイの参照先セルが見つからない | "取得できませんでした" をセットして処理続行 | - |
| W05 | レビュー日時コメントが正しく入力されていません。実施日、開始時刻、終了時刻、レビュー時間を全て入力してください。 | `*` コメントの要素数が 5 未満 | エラーシートに記録して処理続行（他のコメントは処理する） | エラーシートテンプレートをコピーし、エラー内容と該当セルへのハイパーリンクを書き込む |
| W06 | 未登録のカテゴリが使用されています: {カテゴリ記号} | 設定シートに未登録のカテゴリがコメントに使われている | エラーシートに記録して処理続行 | エラーシートテンプレートをコピーし、エラー内容と該当セルへのハイパーリンクを書き込む |

### エラー・処理中断（MsgBox / vbExclamation または vbError）

| コード | メッセージ | 表示タイミング | 処理への影響 | 備考 |
|--------|-----------|-------------|------------|------|
| E01 | レビュー記録一覧のファイルパスが未入力です。 | レビュー記録一覧使用ON かつファイルパス未入力 | 処理中断 | - |
| E02 | 一覧への反映値が未入力です。固定値または、成果物のシート名とセルのどちらかを指定してください。 | 工程・レビュア・レビュイの設定が不完全 | 処理中断 | - |
| E03 | レビュー記録一覧のシート名が未入力です。 | レビュー記録一覧使用ON かつシート名未入力 | 処理中断 | - |
| E04 | レビュー記録一覧ファイルが存在しません。 | 指定パスにファイルが見つからない | 処理中断 | - |
| E05 | レビュー記録一覧ファイルに指定されたシートが存在しません。 | ファイルを開いたがシートが見つからない | 処理中断 | - |
| E06 | レビュー記録票に {シート名} シートが存在しません。 | レビュー記録票の転記先シートが見つからない（ヘッダー・サマリー・指摘一覧の各シート） | 処理中断 | - |
| E07 | 指定されたレビュー回数が正しくありません。 | レビュー記録票のサマリーで回数の順序が不正（前の回が抜けている等） | 処理中断 | - |
| E08 | レビュー対象文書が見つかりません。 | 開いているファイルが対象ファイル名パターンに一致しない | 処理中断（0件通知） | - |
| E09 | メモ削除対象のファイルはありませんでした。 | コメント削除ボタン押下時に対象ファイルがない | 処理中断（0件通知） | - |
| E10 | レビュー結果シート削除対象のファイルはありませんでした。 | シート削除ボタン押下時に対象ファイルがない | 処理中断（0件通知） | - |
| E11 | 処理中にエラーが発生しました。{エラー番号} {エラー内容} | 予期しない VBA 実行時エラー | 処理中断 | - |
| E12 | 指摘分類マッピング設定シートにカテゴリが登録されていません。設定シートにカテゴリを追加してください。 | 指摘分類マッピング設定シートにカテゴリが1件もない | 処理中断 | - |
| E13 | カテゴリ設定が変更されました（{前回}種類 → {今回}種類）。既存のレビュー結果シートのカテゴリ構造と不整合になるため、処理を中断します。【対処方法】1. 設計書Excelの既存のレビュー結果シート（全て）を削除 2. 1回目のレビューから実行をやり直す | 既存レビュー結果シートまたはレビュー記録一覧とカテゴリ構成が不一致（追加・削除・順序変更を含む） | 処理中断 | 検出: 既存の結果シート（ヘッダー行 9行目 B列〜）またはレビュー記録一覧（1行目 AC列〜）のカテゴリリストと現在の設定を比較。対処: 既存のレビュー結果シートを全削除して1回目からやり直す（レビュー記録一覧の削除は不要） |

